@* Початкова сторінка перенаправляє на розклад *@
@page "/"
@inject NavigationManager Nav

@code {
    // Перенаправляє користувача на розклад.
    protected override void OnInitialized()
        => Nav.NavigateTo("/schedule", replace: true);
}
