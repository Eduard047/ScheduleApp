# Керівництво з основних можливостей

Середовище бази даних має використовувати MySQL 8.0.13 або новіший, оскільки схема містить функціональні унікальні індекси.

Цей документ допоможе Вам швидко зорієнтуватися у ключових розділах системи та зрозуміти, як виконувати типові операції. Усі описи стосуються поточної кодової бази BlazorWasmDotNet8AspNetCoreHosted.

> **Виробниче обмеження:** API ще не інтегровано з університетським SSO/JWT і не має рольової авторизації. До впровадження ролей доступ дозволено лише через приватну мережу, VPN або захищений reverse proxy; не публікуйте цей екземпляр у відкритий Інтернет.

## 1. Структура та навігація

У застосунку використовується верхня панель навігації (`TopNav`). Основні маршрути:

| Розділ меню | Маршрут | Призначення |
|-------------|---------|-------------|
| `Розклад → Сформувати план` | `/admin/schedule` | Налаштування діапазону, формування та попередній перегляд плану. |
| `Розклад → Перевірити чернетку` | `/teacher-drafts` | Чернетки викладачів із drag-and-drop, експортом та публікацією в офіційний розклад. |
| `Розклад → Переглянути розклад` | `/schedule` | Перегляд робочого розкладу. |
| `Адмін → Слоти часу` | `/admin/time-slots` | Налаштування тайм-слотів (глобально або для курсу) + позначення обіднього слота. |
| `Адмін → Курси` | `/admin/courses` | Управління навчальними курсами (довідник). |
| `Адмін → Корпуси` | `/admin/buildings` | Довідник будівель. |
| `Адмін → Аудиторії` | `/admin/rooms` | Довідник аудиторій, пов’язаний із будівлями. |
| `Адмін → Групи` | `/admin/groups` | Управління академічними групами. |
| `Адмін → Типи занять` | `/admin/lesson-types` | Класифікатор видів занять + колір на розкладі та прапорці впливу на правила/автоген. |
| `Адмін → Модулі` | `/admin/modules` | Модулі, імпорт із `.docx`, план модуля та теми (заняття) з годинами/прапорцями. |
| `Адмін → Послідовності` | `/admin/module-sequence` | Черга модулів та «заповнювачі» для автогенерації. |
| `Адмін → Кафедри` | `/admin/departments` | Керування кафедрами та їх використанням у викладачах/темах. |
| `Адмін → Викладачі` | `/admin/teachers` | Викладачі, допустимі модулі, навантаження та прив’язка до кафедри. |
| `Адмін → Календар` | `/admin/calendar` | Календарні винятки (робочий/вихідний день) у межах курсу або конкретної групи. |

> **Примітка:** сторінки знаходяться в `BlazorWasmDotNet8AspNetCoreHosted.Client/Pages`, контролери — у `BlazorWasmDotNet8AspNetCoreHosted.Server/Controllers`.

> **Застереження:** сторінка `/admin/lunch` залишена для сумісності, але налаштування обіду виконується у `/admin/time-slots` (позначте потрібний слот як обід).

## 2. Ключові сценарії

### 2.1 Модулі, план і теми (заняття)

- Сторінка `/admin/modules` працює через `AdminModulesController` (`/api/admin/modules`).
- **Імпорт з `.docx`:** доступні **попередній перегляд** і **застосування імпорту**. Після імпорту модулі/теми можна одразу редагувати вручну.
- **План модуля:** відкривається у модальному вікні поверх сторінки; план зберігається окремо на курс і використовується для розрахунку цільових годин та автогенерації.
- **Теми модуля (заняття):**
  - код у форматі на кшталт `1.1.1.1`, тип заняття, кафедра (за потреби), аудиторні/самостійні години;
  - код і порядок у довіднику не задають календарну чергу: теми з поточного вікна навчального плану можна міняти місцями, якщо не перевищено їхні доступні години;
  - прапорець **«Міжзборові заходи»** — такі теми не беруться в автогенерацію, а модуль із *лише* міжзборовими темами буде пропущений автогеном із попередженням;
  - кнопка для самостійних годин **«У розкладі / Не розкладати»** керує тим, чи потрібно розкладати самостійні години теми під керівником.

### 2.2 Послідовність модулів

- `/admin/module-sequence` дозволяє формувати перелік «головних» модулів (черга) та позначати додаткові модулі як заповнювачі.
- Збереження викликає `AdminModuleSequenceController` (у `Server/Controllers/Admin`).
- Після натискання «Зберегти» дані потрапляють у таблиці послідовності (черга + fillers) і використовуються автогенерацією.
- Послідовність блоків модулів є окремим правилом і залишається обов'язковою: блоки проходять у заданому порядку без повернення до вже завершеного блока. Вільний календарний порядок тем у межах поточного вікна навчального плану це правило не змінює.

### 2.3 Календарні винятки та статус дня

- `/admin/calendar` зберігає винятки з ознакою **робочий/вихідний**.
- Виняток створюється **в межах курсу**, а за потреби може бути звужений до **конкретної групи**.
- Розклад, чернетки та автогенерація враховують винятки під час визначення робочих днів тижня.
- Для автогенерації у `/admin/schedule` спочатку застосовується обраний пресет днів тижня; дні поза пресетом не використовуються.
- Субота й неділя за замовчуванням неробочі. Сам вибір пресета `Пн–Сб` лише дозволяє розглядати суботу, але не робить її робочою: додайте для потрібної дати календарний виняток **«Робочий день»** або свідомо використайте режим дозволу на неробочі дні, якщо він доступний у конкретній операції.

### 2.4 Слоти часу та обід

- `/admin/time-slots` підтримує:
  - **глобальні** слоти (для всіх курсів) або **курсові** (перекривають глобальні для обраного курсу);
  - перегляд **ефективних** слотів (курсові, якщо є, інакше глобальні);
  - позначення **одного** слота як обіднього (`IsLunch`).
- Автогенерація використовує слоти 1-в-1, перекриття активних слотів не допускаються.
- Для типів занять із прапорцем **«Бажано першим у тижні»** задається верхня межа слота. Рекомендований профіль використовує 6-ту пару включно.

### 2.5 Чернетки викладачів

- `TeacherDrafts.razor` (`/teacher-drafts`) забезпечує drag-and-drop інтерфейс:
  - перемикання тижнів, фільтрація за викладачем/групою/аудиторією/курсом;
  - експорт в Excel: `GET /api/teacher-drafts/export` з параметрами `weekStart/teacherId/groupId/roomId` повертає `Rozklad-yyyyMMdd.xlsx`;
  - **режим без обмежень** («редагування без обмежень») дозволяє зберігати зміни, ігноруючи частину валідацій (конфлікти все одно збираються та відображаються);
  - якщо тема має прив’язку до кафедри, список викладачів у редакторі обмежується цією кафедрою (або скидає невідповідний вибір);
  - таблиця має «липку» шапку та зафіксовані колонки **День/Година** для зручної роботи з великими тижневими сітками.
- Публікація тижня переносить затверджені чернетки у `ScheduleItems` з повторною перевіркою правил.
- Внутрішній статус `Published` у таблиці чернеток означає **«схвалено»**, а не вже перенесено до офіційного розкладу. Схвалені рядки незмінні для звичайного редагування й очищення; їх можна опублікувати без змін або явно видалити після підтвердження в режимі без обмежень.
- Тижнева публікація обробляє цілісний пакет чернеток усіх статусів, повторно перевіряє hard rules і виконується атомарно: у разі помилки офіційний розклад не змінюється.
- Кожен рядок розкладу й чернетки має ревізію. Якщо інший диспетчер уже змінив або видалив відкритий запис, сервер відхиляє застарілу операцію без перезапису нових даних; оновіть таблицю та повторіть дію.
- Стан конфліктів зберігається у `localStorage` (`teacherDrafts.conflicts.v1`), щоб Ви могли повертатися до виявлених проблем.

### 2.6 Формування плану на діапазон дат

- На сторінці `/admin/schedule` використовується послідовність **Сформувати план → переглянути попередній план → Прийняти план** (`Plan → Preview → Apply`). До натискання **«Прийняти план»** робочі чернетки не змінюються.
- Для генерації за модулями:
  1. оберіть курс;
  2. позначте модулі, які мають бути у вибраному діапазоні;
  3. вкажіть загальну кількість занять на одну групу за весь діапазон для кожного модуля (налаштування зберігаються у сховищі браузера).
- Лічильник **«У розкладі та чернетках»** підсумовує заняття за всім вибраним діапазоном. Наприклад, значення `8` у двотижневому запуску означає вісім занять на групу за два тижні разом, а не по вісім занять щотижня.
- Автогенерація балансує навантаження, враховує слоти часу, календарні винятки, доступні аудиторії й викладачів та показує причини незаповнених місць.
- Календарний порядок тем у межах поточного вікна навчального плану вільний. Теми поза цим вікном не витрачають задану квоту, а послідовність блоків модулів із `/admin/module-sequence` зберігається.
- Типи занять із прапорцем **«Бажано першим у тижні»** (для робочого сценарію — лекції) ставляться верхнім блоком дня. У рекомендованому профілі лекції мають бути не пізніше 6-ї пари включно; інші типи занять розміщуються нижче лекційного блоку.

### 2.7 Кафедри та фільтрація викладачів

- `/admin/departments` дозволяє створювати/редагувати кафедри (активна/неактивна).
- Кафедра може бути прив’язана:
  - до викладача (`/admin/teachers`);
  - до теми модуля (`/admin/modules` → теми).
- У чернетках викладачів (та пов’язаних формах) кафедра теми може обмежувати вибір викладача для цієї теми.

### 2.8 Викладачі: робочі вікна та самостійні роботи

- У `/admin/teachers` задаються робочі вікна по днях тижня; під час перевірки розкладу та чернеток система попереджає, якщо заняття виходить за ці межі.
- Блок **Керівництво самостійними роботами** визначає викладачів для модулів, які можуть вести самостійні години тем з прапорцем **У розкладі**; ці зв'язки враховуються автогенерацією.

### 2.9 Корпуси та час на дорогу

- На `/admin/buildings` окремо задається час переходу між корпусами; для нових корпусів автоматично створюються маршрути за замовчуванням (20 хв).
- Перевірки розкладу та чернеток блокують заняття, між якими бракує часу на перехід, а автогенерація враховує цей параметр як штраф.

## 3. Рекомендований робочий процес

1. **Підготуйте довідники**: курси, кафедри, корпуси, аудиторії, групи, типи занять і час на дорогу між корпусами.
2. **Налаштуйте слоти часу** на `/admin/time-slots` (за потреби — окремо для курсів) і позначте обідній слот.
3. **Підготуйте модулі** на `/admin/modules`: імпорт із `.docx` або ручне створення, план модуля, теми з годинами й прапорцями.
4. **Задайте послідовність модулів** на `/admin/module-sequence` (черга + fillers).
5. **Задайте викладачів** на `/admin/teachers`: кафедра, допустимі модулі, налаштування керівництва самостійними роботами та робочі вікна.
6. **Зафіксуйте календарні винятки** на `/admin/calendar` (для курсу або окремої групи).
7. **Сформуйте план** через `/admin/schedule`, перегляньте попередній результат і лише після перевірки натисніть **«Прийняти план»**. Потім перевірте чернетку у `/teacher-drafts`, за потреби відредагуйте або експортуйте її та опублікуйте у робочий розклад.

## 4. Технічні довідки

- **EF Core**: робота з міграціями описана в `README.md`. Перед запуском сервера після оновлень виконайте `dotnet tool restore`, а потім `dotnet ef database update --project BlazorWasmDotNet8AspNetCoreHosted.Server` з кореня репозиторію. Для міграції ownership/lease автогенерації обов'язково спочатку припиніть приймання нових job, дочекайтеся завершення або скасуйте активні job та зупиніть усі instance старої версії; rolling-оновлення зі старими активними генераторами не підтримується. Після застосування міграції навчального періоду відкрийте **Адміністрування → Курси** та задайте дату початку поточного періоду для кожного курсу — до цього автогенерація буде навмисно заблокована.
- **Правила валідації**: `Server/Application/RulesService.cs` (перетини, робочі дні, обмеження аудиторій/викладачів тощо).
- **DTO та контракти**: `Shared/DTOs`.
- **Метадані для UI**: `MetaController` повертає агреговані довідники (`api/meta`), зокрема **кафедри** та **календарні винятки** зі scope `CourseId/GroupId`.
- **Клієнтські API-сервіси**: `Client/Services/AdminApi.cs` (адмін) та `Client/Services/TeacherDraftsApi.cs` (чернетки та автогенерація).

## 5. Корисні поради

- Якщо потрібно «з нуля» перевірити схему, створіть окрему одноразову локальну БД і виконайте `dotnet ef database update --project BlazorWasmDotNet8AspNetCoreHosted.Server` — міграції містять повну структуру.

> **Застереження:** не видаляйте робочу БД для перевірки міграцій. Оновлення production-схеми виконуйте лише після резервного копіювання та перевірки міграцій у staging-середовищі.

## 6. Актуальна автогенерація та контроль якості

- Автогенерація у `/admin/schedule` підтримує довгі діапазони: тиждень, два тижні, три тижні, місяць і весь курс. Для великих запусків використовується job-модель зі статусом прогресу.
- Перед новим семестром у **Адміністрування → Курси** оновіть дату початку навчального періоду. Історія до цієї дати не враховується як виконаний план, використана тема або поточне навантаження; запуск без дати чи до неї відхиляється і клієнтом, і сервером.
- Стан запуску зберігається у `AutoGenJobRuns` разом із hash запиту, owner/attempt і lease, тому кілька replica бачать один авторитетний статус, результат або помилку.
- Робочим контрактом є `POST /api/teacher-drafts/autogen/jobs`; старі синхронні маршрути вимкнені та повертають `410 Gone`. Для `Generate` і `Fill` обов'язково передавайте `previewOnly: true`: прямий запис через job API навмисно відхиляється з `400 Bad Request`, тому старі клієнти та скрипти потрібно оновити разом із сервером.
- `Generate` і `Fill` реалізують сценарій `Plan → Preview → Apply`: спочатку формують точний план без зміни робочих чернеток, потім користувач переглядає його і окремо підтверджує застосування. Деталі доступні через `GET /api/teacher-drafts/autogen/jobs/{jobId}/plan`, а `POST .../{jobId}/apply` і `POST .../{jobId}/rollback` застосовують або відкочують план із перевіркою версії, ревізій рядків, конфігурації та hard rules. Готовий план чинний 24 години; після застосування відкіт доступний 7 днів, якщо отримані рядки ніхто не змінював. Найновіший доступний відкіт можна знайти через `GET /api/teacher-drafts/autogen/plans/latest-rollbackable?courseId={courseId}`. Прострочені записи зберігаються ще до 30 днів для діагностики, після чого видаляються фоновим очищенням.
- Сторінка `/admin/schedule` показує кількість і точний перелік додавань, змін та видалень до підтвердження. Після перезавантаження вона відновлює актуальний стан плану з сервера; конфлікт або зміна будь-якого релевантного входу вимагає нового preview.
- Черга є DB-authoritative. Створення job атомарне й fail-closed, повторний `ClientJobId` перевіряється за hash параметрів, а owner/attempt/lease з heartbeat за часом БД не дозволяють двом replica виконувати один job. Активний legacy-запис без ownership-даних блокує нові запуски й не змінюється автоматично. Віддалене скасування спочатку фіксується в БД і підхоплюється власником; між різними job фактичну генерацію серіалізує окремий для БД MySQL named lock. Під час штатної зупинки сервер припиняє приймати нові запуски, надсилає активним завданням сигнал скасування та очікує їх завершення в межах тайм-ауту хосту. Якщо код генератора ігнорує скасування, watchdog припиняє heartbeat і зупиняє host, щоб оркестратор перезапустив instance та закрив session з lock. Після аварійного перезапуску незавершений job не поновлюється: після завершення lease він переходить у `Failed` з позначкою про невідомий результат, без автоматичного replay.
- Для виробничого середовища використовуйте налаштований у застосунку MySQL. SQLite підтримується для локальної розробки та тестів в одному процесі: через обмеження одного writer довга атомарна транзакція тимчасово призупиняє DB-heartbeat, використовує захисний lease до шести годин і не гарантує міжпроцесне скасування до завершення транзакції.
- Години модулів задають спільну потребу однієї групи за весь вибраний діапазон і не множаться на кількість календарних тижнів. Лічильники виконання використовують той самий діапазон. Усі тижні Generate/Fill виконуються атомарно: помилка або скасування відкочує весь запуск, а не залишає частково заповнений перший тиждень.
- Preflight-перевірка не записує чернетки, але послідовно симулює весь діапазон в одній транзакції й повертає дефіцити ресурсів і рекомендації: нестачу слотів, викладачів, аудиторій, проблеми з корпусами, переходами та доступними годинами тем. Після формування звіту транзакція повністю відкочується.
- Після первинного greedy-планування виконується repair/fill pass: він пробує дозаповнювати прогалини, зсувати заняття і приймає зміни тільки в межах hard rules.
- Перед локальним repair/fill pass виконується точний поліноміальний successive-shortest-path min-cost max-flow за всіма залишковими потребами. Він максимізує кількість розміщених занять, потім мінімізує вартість призначень і застосовує стабільний лексикографічний tie-break; увесь план перевіряється атомарно, а спільний для групи та дати бюджет рахує детерміновані розгортання вершин.
- Перед збереженням нових чернеток запускається спільний hard-rule validator. Він перевіряє календар, активні часові слоти, викладачів, аудиторії, перетини груп, місткість аудиторій, дозволені спільні потоки та послідовність блоків модулів. Календарний порядок тем усередині модуля не обмежується.
- Після генерації орієнтуйтесь не тільки на кількість створених чернеток, а й на структуровані `warningDetails` (`code`, `severity`, `category`, `message`, `context`) та `gapReasons`: `teacher`, `room`, `travel`, `topic-order`, `module-block`, `limit`, `search-limit`, `shared-flow`, `unknown`, `other`. `topic-order` — збережена для сумісності назва коду нестачі доступних годин тем, а не вимога календарної черги. Текстовий масив `warnings` також збережено для сумісності; поля `constraintCode`, `searchLimitReached` і `diagnostics` пояснюють конкретне обмеження та використаний бюджет пошуку.
- Контрольний сценарій L-3 охоплює `01.09.2026`–`12.09.2026`, `Пн–Сб`, групи `9301`–`9307` і квоти на одну групу: `1=7, 2=9, 3=36, 4=10, 5=6, 6=5, 8=7, 10=5, 12=5, 13=3`. Разом це 93 пари на групу та 651 пара для семи груп, із них 252 — модуль 3. Локальний тест перевіряє нуль дефіцитів і пропусків після інтегрованого дозаповнення, точні квоти, порядок модулів, вільний календарний порядок тем у поточному вікні плану, лекційний блок до 6-ї пари включно та повний цикл `Plan → Preview → Apply`.
- У CI профілі `AutogenQuality` запускають зафіксовану синтетичну матрицю з KPI-порогами та стабільними fingerprints, зокрема 18-тижневий refill-сценарій на 324 заняття. На pull request і push виконується один smoke-прохід, а за розкладом — три послідовні проходи для виявлення недетермінізму; нуль знайдених quality-тестів вважається помилкою. Residual solver додатково звіряється з незалежним brute-force oracle, а production guardrails перевіряють lease/idempotency/cancel між двома service instance та реальну серіалізацію MySQL named lock у CI.
